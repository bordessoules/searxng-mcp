# MCP Gateway Dependencies (Host Installation)
# Slim version - no torch/CUDA, external services for heavy lifting

# Core MCP framework
mcp[cli]>=1.0.0

# HTTP client for external APIs
httpx>=0.27.0

# Environment configuration
python-dotenv>=1.0.0

# Web content extraction (lightweight)
markdownify>=0.11.0
trafilatura>=1.6.0
readability-lxml>=0.8.1

# Server (for SSE transport)
uvicorn>=0.30.0

# Note: Playwright is installed separately via npm
# npm install -g @anthropic-ai/claude-code  (includes playwright)
# Or: npx @playwright/mcp@latest --help
